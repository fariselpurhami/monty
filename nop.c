#include "monty.h"

/* prototype */

void nop(stack_t **stack, unsigned int lineCount);

/**
* nop - LET'S WRITE A FUCNTION THAT WILL NOT DO NOTHINS.
* @stack: IT'S A POINTERS THAT COMES UP WITH STACKS P&W.
* @lineCount : IT'S A POINTER THAT HAVE LINES FOR ERROR.
* Return: NOTHING
*/
void nop(stack_t **stack, unsigned int lineCount)
{
	(void) stack;
	(void) lineCount;
}
